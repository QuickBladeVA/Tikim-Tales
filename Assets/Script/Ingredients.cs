using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public enum Ingredient { Water, Vegtable, Meat }
public enum Holding {Left, Right}
public enum Look { Up, Down, Left, Right }
public enum Minigame { Pour, Cook, Chop, Stir }
